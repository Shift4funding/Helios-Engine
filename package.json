{
  "name": "bank-statement-analyzer-api",
  "version": "1.0.0",
  "description": "API for analyzing bank statements using LLMs, called by Zoho.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "express",
    "nodejs",
    "bank-statement",
    "llm",
    "zoho"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",         // For making HTTP requests to LLM APIs
    "dotenv": "^16.4.5",       // For managing environment variables
    "express": "^4.19.2",      // Web framework
    "multer": "^1.4.5-lts.1",  // For handling file uploads
    "pdf-parse": "^1.1.1"      // For parsing text from PDF files (can be replaced/augmented)
  },
  "devDependencies": {
    "nodemon": "^3.1.0"        // For automatically restarting the server during development
  }
}
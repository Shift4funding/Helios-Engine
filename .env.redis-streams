# Redis Streams Environment Configuration
# Bank Statement Analyzer with AI Categorization Cache Integration

# Application Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/bank-statement-analyzer
REDIS_URL=redis://localhost:6379

# Redis Streams Configuration
ENABLE_WORKERS=true
ENABLE_CLUSTERING=false
MAX_WORKERS=4

# Worker Configuration
STATEMENT_WORKERS=2
CATEGORIZATION_WORKERS=3
RISK_WORKERS=2

# Security Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
API_KEY_SECRET=your-api-key-secret-change-in-production

# Rate Limiting
RATE_LIMIT=100

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# AI Categorization Cache Configuration
AI_CACHE_ENABLED=true
AI_CACHE_TTL=86400
AI_CACHE_MAX_SIZE=10000

# External API Configuration (Optional)
PERPLEXITY_API_KEY=your-perplexity-api-key
ZOHO_CLIENT_ID=your-zoho-client-id
ZOHO_CLIENT_SECRET=your-zoho-client-secret
ZOHO_REFRESH_TOKEN=your-zoho-refresh-token
ZOHO_API_DOMAIN=https://www.zohoapis.com

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/application.log

# Performance Configuration
MAX_FILE_SIZE=50mb
REQUEST_TIMEOUT=30000

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30000
METRICS_COLLECTION_INTERVAL=60000
